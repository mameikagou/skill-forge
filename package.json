{
  "name": "skill-forge",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "skills/*",
    "packages/*"
  ],
  "scripts": {
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
