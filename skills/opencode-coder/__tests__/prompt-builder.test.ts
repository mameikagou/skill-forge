import { test, expect, describe } from "bun:test";
import { buildPrompt } from "../scripts/prompt-builder";

describe("buildPrompt", () => {
  test.todo("single file prompt");
  test.todo("multi file prompt");
});
